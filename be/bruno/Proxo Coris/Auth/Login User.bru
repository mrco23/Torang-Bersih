meta {
  name: Login User
  type: http
  seq: 2
}

post {
  url: {{base_url}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "user1@example.com",
    "password": "User123!"
  }
}

vars:pre-request {
  base_url: http://127.0.0.1:5000/api
}

script:post-response {
  if (res.status === 200) {
    bru.setVar("user_access_token", res.body.data.access_token);
    bru.setVar("user_refresh_token", res.body.data.refresh_token);
  }
}
