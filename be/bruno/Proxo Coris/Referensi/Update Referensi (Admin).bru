meta {
  name: Update Referensi (Admin)
  type: http
  seq: 3
}

put {
  url: {{base_url}}/referensi/jenis-kolaborator/{{ref_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{admin_access_token}}
}

body:json {
  {
    "nama": "Perusahaan Updated"
  }
}
